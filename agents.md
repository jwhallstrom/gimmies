# agents.md

Operational guide for building, running, and deploying the Gimmies Golf app. Optimized for humans *and* automation agents so they reuse existing processes instead of spawning duplicate dev/build tasks.

---
## 1. Purpose
Central place to:
- Reuse the existing VS Code tasks (Dev Server, Build (prod))
- Avoid starting multiple dev servers / builds
- Standardize production build + AWS Amplify Hosting deployment
- Handle caching + PWA/service worker updates safely
- Provide copy-paste commands for Windows PowerShell + CI

---
## 2. Multi-App Architecture (Updated Feb 2026)

This repo hosts **three separate apps** deployed via Amplify Hosting:

| Branch | Subdomain | App | Build Output |
|--------|-----------|-----|--------------|
| `master` | app.golfwithgimmies.com | Main Gimmies App | `dist/` |
| `landing` | golfwithgimmies.com + www | Marketing Landing | `apps/landing/dist/` |
| `tournaments` | play.golfwithgimmies.com | Tournaments PWA | `apps/tournaments/dist/` |

**Key points:**
- Each branch auto-deploys to its subdomain when pushed
- All three branches are production-impacting
- Apps share the same Amplify backend (Cognito, AppSync, DynamoDB)

---
## 3. Key Build Concepts
| Mode | Command Path | When to Use | Notes |
|------|--------------|-------------|-------|
| Dev  | `npm run dev -- --host` | Local iteration | Uses Vite dev server, auto reload |
| Prod Build | `npm run build` | Before deploy / verifying prod bundle | Outputs to `dist/` |
| Preview | `npx vite preview` | Serve built assets locally | VS Code task available |

Tasks already defined (see `.vscode/tasks.json` auto-generated by VS Code):
- **Dev Server**: background task for `npm run dev -- --host`
- **Build (prod)**: runs the production build
- **Build + Dev (manual test)**: sequential build, then starts dev (mainly for sanity after a clean build)
- **Preview (prod build)**: serves the built `dist/` locally via `npx vite preview --host`

> Agents / scripts SHOULD check for an existing terminal/task labeled `Dev Server` before starting another. Duplicate dev servers waste ports + memory.

---
## 4. Reusing Existing Tasks (Agent Behavior Recommendations)
1. Enumerate current terminals; if one contains `npm run dev` and is still active, reuse it.
2. Only trigger **Build (prod)** if:
   - A deployment is requested OR
   - Source files changed since last build (optional heuristic: compare `dist/` timestamps)
3. Never run `npm run build` concurrently with another build.
4. After build finishes, deploy from the same artifact directory (`dist/`). Do NOT rebuild just to deploy.

Pseudo-logic:
```pseudo
if taskExists('Dev Server') and taskIsRunning: skip start
else: runTask('Dev Server')

if needDeploy:
  runTask('Build (prod)')  # wait for completion
  deployDist()
```

---
## 5. Production Build Steps (Manual)
```powershell
# From repo root
npm ci            # or npm install (ci preferred in CI environments)
npm run build     # emits dist/
```
Output artifacts of interest:
- `dist/index.html`
- Hashed JS/CSS under `dist/assets/`
- PWA files: `sw.js`, `workbox-*.js`, `manifest.webmanifest`

### 5.1 Amplify Hosting (Auto-deploy via GitHub)
AWS Amplify Hosting auto-builds and deploys all three app branches:

| Branch | Subdomain | What it builds |
|--------|-----------|----------------|
| `master` | app.golfwithgimmies.com | Main app (`dist/`) |
| `landing` | golfwithgimmies.com + www | Landing page (`apps/landing/dist/`) |
| `tournaments` | play.golfwithgimmies.com | Tournaments PWA (`apps/tournaments/dist/`) |

**How to deploy:**
1. Push to the appropriate branch (`master`, `landing`, or `tournaments`)
2. Amplify auto-triggers build within ~1 minute
3. Build typically takes 8-12 minutes
4. Check status: Amplify Console -> Build history

**Sync branches after shared changes:**
```powershell
git checkout landing; git merge master --no-edit; git push origin landing
git checkout tournaments; git merge master --no-edit; git push origin tournaments
git checkout master
```

See `AMPLIFY_HOSTING_SETUP.md` for full deployment documentation.

---
## 6. PWA / Service Worker Notes
- Build generates `sw.js` each time; users may see prior cached version until next navigation.
- Keeping `sw.js` on a short cache (<=5 min) accelerates adoption of new releases.
- If critical hotfix: bump a dummy query param in `index.html` referencing the register script OR manually invalidate.
- Each app (main, tournaments) has its own PWA manifest and service worker.

---
## 7. Safe Agent Behaviors Summary
| Action | MUST | MUST NOT |
|--------|------|----------|
| Start dev | Check if running first | Spawn duplicates |
| Build | Run once per deploy | Rebuild after a fresh build before deploy |
| Deploy | Use existing `dist/` | Upload partial / stale artifacts |
| Push | To correct branch | Push directly to production branches without approval |

---
## 8. Troubleshooting
| Symptom | Cause | Fix |
|---------|-------|-----|
| New UI not visible | Browser using old SW cache | Hard refresh (Ctrl+F5) or wait cache TTL |
| 404 on deep link | SPA routing not configured | Check Amplify rewrites in build settings |
| Layout different in prod | Tailwind purge misconfig | Ensure classes are static strings or safelisted |
| Double dev ports | Multiple dev tasks started | Reuse existing terminal labeled Dev Server |
| Build fails on branch | Branch-specific amplify.yml issue | Check build output directory matches branch |

---
## 9. CI/CD Sketch (Updated)
```yaml
steps:
  - uses: actions/checkout@v4
  - uses: actions/setup-node@v4
    with: { node-version: 20 }
  - run: npm ci
  - run: npm run build
  # E2E on dev server
  - run: npx playwright install --with-deps
  - run: npm run e2e
  # E2E against built preview (closer to prod)
  - run: npm run e2e:preview
```

---
## 10. Quick Reference (Copy-Paste)
```powershell
# Build (local)
npm run build

# Deploy (Amplify Hosting)
# Just push to the appropriate branch - Amplify auto-builds and deploys

# Check build status (requires AWS CLI)
aws amplify list-jobs --app-id dtsoc1sfk1bk8 --branch-name master --max-items 1

# Sync all branches after master changes
git checkout landing; git merge master --no-edit; git push origin landing
git checkout tournaments; git merge master --no-edit; git push origin tournaments
git checkout master
```

---
**End of agents.md**
